#include <iostream>
#include <cstdlib>
#include <ctime>
using namespace std;

int main(){
    srand(time(0));
    int usuario, computadora = rand() % 3;
    
    cout << "0 = piedra, 1 = papel, 2 = Tijera: " << endl;
    cin >> usuario;
    
    cout << "Computadora eligio: " << computadora << endl;
    
    if (usuario == computadora){
        cout << "Empate!!" << endl;
    } else if ((usuario == 0 && computadora == 2) || (usuario == 1 && computadora == 0) || (usuario == 2 && computadora == 1)){
        cout << "Ganastes!! ES MUUY DIFICIL GANARLE A ESTA COMPUTADORA!!" << endl;
    } else {
        cout << "Perdistes!! MAS SUERTE LA PROXIMA!!";
    }
    
    return 0;
}
